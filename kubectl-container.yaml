kind: Pod
metadata:
  name: kubectl
  namespace: jenkins-ns
spec:
  containers:
  - name: kaniko
    image: bitnami/kubectl
    imagePullPolicy: Always
    command:
      - "sleep"
      - "240"
    tty: true